{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "docker",
  "database": "api_auth",
  "cli":{
      "migrationsDir":"src/database/migrations"
  },
  "entities": [
     "src/app/modules/*.ts"
  ],
  "migrations": [
     "src/database/migrations/*.ts"
  ],
  "subscribers": [
     "src/subscriber/**/*.ts"
  ]
}